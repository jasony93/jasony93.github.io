# 2차 기능(F1~F8) 테스트 리포트

## 이력

| 날짜 | 회차 | 결과 요약 |
|---|---|---|
| 2026-08-04 | 1차 | PASS 43 / FAIL 0 / BLOCKED 5. 계산·데이터·프리렌더·회귀 전 항목 PASS. BLOCKED 5건(고유 기준 - 3.2절 #12와 3.7절 #34는 3.10절 B1·B2의 상호참조)은 전부 브라우저 실조작 항목 - 본 세션에서 브라우저 자동화 도구가 소실되어 실측 불가 (개발팀 1차 테스트와 동일한 환경 제약. 로직 계층은 출하 코드 실행으로 전부 검증함) |

## 1. 테스트 대상

- 코드: `src/fetch_data.py`(v2 확장), `src/web/app.js`(F1/F3/F5/F6 클라이언트 계층), `src/build_pages.py`(F2/F4/F7/F8 프리렌더), `src/config/tickers.json`(f7 블록 + SKHY 수동 필드), `src/web/styles.css`(신규 색 변수 4종), `.github/workflows/update-data.yml`(intraday 스케줄), `src/tests/`(81건)
- 판정 기준 PRD: `docs/planning/2026-08-04-v2-features-prd.md` (F1~F8 각 절 완료 기준. 9절 확인표는 개발팀 기입 완료분을 독립 재조회로 재검증)
- 설계 문서: `docs/development/2026-08-03-adr-premium-design.md` 8절(v2) + 제약 13~16 (제약 명시 항목은 제약 확인으로 분류)
- 중점: 개발팀 1차 테스트 미확인 항목(브라우저 실조작) - 본 세션에서도 브라우저 도구 부재로 실측 불가 판명. 대체로 출하된 app.js를 Node에서 직접 실행하는 QA 자체 하니스(개발팀 하니스와 무관하게 신규 작성)로 로직 계층을 전부 검증하고, 실조작 항목만 BLOCKED 처리.

## 2. 테스트 환경과 실행 방법

- 환경: Windows 11 Home, Python 3.11.9, yfinance 1.5.2, Node.js v24.18.0 (app.js 하니스용). 실행 시각 2026-08-04 18:04~18:40 KST - **LSE만 개장 중** (KRX·TWSE·미국 폐장. SMSN 장중 라벨 실측에 활용. KRX 장중 시간대 실측은 실행 시각상 불가 - 세션 판정 로직을 모의 시각 15케이스로 검증해 갈음)
- 실행 절차:
  1. `python -m unittest discover -s src/tests -p "test_*.py"` (81건)
  2. history 11파일 SHA-256 기록 -> `python src/fetch_data.py --intraday` -> 해시 재대조 (F2 불변성)
  3. `python src/fetch_data.py`(full, LSE 개장 중) + `python src/build_pages.py` -> 이후 콘텐츠 검증은 이 상태 기준
  4. `python src/tests/verify_samples.py` (기존 실데이터 검증 회귀)
  5. `docs/testing/scripts/2026-08-04-qa-v2-verify.py` - F7 독립 재조회(11심볼 22조회)·수기 재계산, F4/F8 수기 대조, F2 라벨, 폴백·각주 (67항목)
  6. `docs/testing/scripts/2026-08-04-qa-v2-calc.py` - F3/F5/F6 기대값을 PRD 규칙으로 파이썬 독립 산출 -> `2026-08-04-qa-appjs-harness.js` - 출하된 app.js를 Node로 로드해 aggregate/smaMap/computeWindow를 실데이터로 실행, 기대값과 대조 (34항목)
  7. `docs/testing/scripts/2026-08-04-qa-v2-misc.py` - 세션 판정 모의 시각 15케이스, F6 거래량 야후 독립 재조회 5건, WCAG 신규 4변수 x 2테마, 워크플로 YAML, 정적 존재 확인 (40항목)
  8. F2 실패 주입: fetch_close_volume 몽키패치로 SKM만 조회 실패시켜 run_intraday 실행 -> 실패 처리 확인 -> 정상 --intraday 재실행으로 원상복구 (fetch_error 잔존 없음 검증)
  9. `docs/testing/scripts/2026-08-04-qa-seo-verify.py` - 기존 D1~D4 회귀 92항목
- 소스 코드 무수정. 오류 주입은 몽키패치(프로세스 내)만 사용했고 데이터 상태는 정상 재실행으로 복구, 최종 build로 페이지-데이터 일관성 확보.
- 브라우저 자동화 도구가 본 세션에서 소실되어(이전 회차에서 사용한 도구 미제공) 화면 실조작 검증은 불가 - 해당 항목 BLOCKED (4절).

## 3. 항목별 결과

### 3.1 공통·회귀

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 1 | 단위 테스트 81건 | unittest discover | 81/81 | 81/81 OK (0.12초) | PASS |
| 2 | 실데이터 검증 회귀 | verify_samples.py | 전체 PASS | 전체 PASS, exit 0 (수기 대조 9/9, 환율 11/11, 이월 4/4, 메타 무결성) | PASS |
| 3 | 파이프라인 full + build | 직접 실행 | 11/11 + 14페이지 | fetch 11/11 (rows 18~5,881), build exit 0 | PASS |
| 4 | SEO/프리렌더 회귀 (D1~D4) | 기존 92항목 스위트 재실행 | 전 항목 유지 | **92/92 PASS** - v2 변경 후에도 메타태그·sitemap·JSON-LD·가이드 전부 무손상 | PASS |
| 5 | 메인 카드 11종 프리렌더 = meta.json | 수치·링크·스파크라인 대조 | 일치 | 11/11 일치, 스파크 11개 | PASS |
| 6 | WCAG 신규 색 변수 4종 x 2테마 (PRD 공통 원칙 5, 설계 8.2절) | styles.css에서 색상 추출, 상대휘도 독립 재계산, 설계 기록값과 대조 | 비텍스트 3:1 이상 | 8/8 통과, 전부 설계 기록값과 0.05 이내 일치. 최저 light --vol-bar 4.29:1 | PASS |
| 7 | 워크플로 (F2 스케줄) | PyYAML 파싱 + cron 대조 | PRD F2 표와 부합 | 유효. cron 4개: 일일 확정 21:30 UTC + 아시아 0-6시 매시 + 마감 확정 06:40 + 미국 14-21시 매시 (전부 UTC, 평일). --intraday 플래그 확인. 런던 전용 세션 미포함은 PRD가 위임한 재량 (설계 8.1절 판단 기록 있음) | PASS |
| 8 | 의존성 0 유지 (F1 완료 기준) | 전 페이지 script 태그·CDN 검사 | 외부 라이브러리 없음 | 외부 스크립트 0건, app.js 단일 | PASS |

### 3.2 F1: 차트 드래그 패닝

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 9 | 윈도우 로직 (출하 app.js를 Node로 실행, KB 5,881행 실데이터) | computeWindow를 직접 호출: 1Y 윈도우, 100일 과거 이동, 과거·미래 경계, 최신 복귀, MAX, 짧은 종목 | PRD 명세대로 이동·클램프·복귀·비활성 | 12/12: 1Y 258포인트, 과거 이동 시 isPanned=true·윈도우 길이 유지, 과거 경계에서 데이터 시작(2003-12-01)에 클램프(오버스크롤 없음), 미래 경계에서 최신 복귀(endMs=null), 기간 탭 재선택 로직 = endMs 초기화 코드 확인, MAX 비활성, SKHY(18행) 1Y 비활성 | PASS |
| 10 | 키보드 이동량 = 윈도우 길이의 10% | 핸들러 수식 재현 계산 | 1Y = 36.6일 스텝 | 37일 (반올림 일치) | PASS |
| 11 | 터치 판별 준비 (touch-action, 임계값) | 코드 정적 확인 | pan-y + 수평 10px 임계값 + 세로 양보 | svg.style.touchAction="pan-y", TOUCH_THRESHOLD=10, pending->pan/scroll 분기 존재 | PASS |
| 12 | 드래그·터치·키보드 **실조작** + 패닝 중 콘솔 에러 0 + "최신으로" 버튼 클릭 | 브라우저 실측 필요 | - | 브라우저 도구 부재 - 4절 BLOCKED #1 | BLOCKED |

### 3.3 F2: 장중(지연) 시세

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 13 | --intraday 실행 시 history 불변 (완료 기준 4) | 실행 전 11파일 SHA-256 기록 -> 실행 -> 재대조 | 해시 전부 불변 | **11/11 불변** (changed 0, meta만 갱신·mode=intraday) | PASS |
| 14 | 차트 분리: full 실행 중 개장 시장의 진행 중 행 제외 | LSE 개장 중(18:07) full 실행 후 SMSN 검사 | 히스토리 8/4 행은 확정 종가 이월, 스냅샷만 장중가 | 히스토리 8/4 p_dr=4188(이월, ff_dr=true) vs 스냅샷 p_dr=4232(intraday=true) - 진행 중 가격이 차트에 미유입 | PASS |
| 15 | 세션 판정 - 실측 + 모의 시각 15케이스 | 18:05 실행 결과(SMSN만 장중) + is_market_open/drop_in_progress에 모의 시각 주입 | KST 벽시계 세션 표대로 판정 | 실측: 11종 중 SMSN만 "장중" (LSE 개장 시간과 정합). 시뮬레이션 15/15: KST 11시(KRX·TWSE open/미·런 closed), 15:39/15:40 경계, 자정 걸침(미국 03시 open·06:10 closed, 런던 01시 open), 주말(토 새벽 미국장만 open, 일 closed), 06:30 전 시장 폐장(히스토리 순수성), KRX 장중 당일 행 제거/마감 후 유지 | PASS |
| 16 | 지연 라벨 표준 (완료 기준 1~3) | 원시 HTML에서 PRD 형식 문자열 대조 | 배지·값별 라벨·각주 형식 일치 | SMSN: "장중 지연 시세 (수집 18:07)" 값 라벨 + "장중 지연 시세 기준 - 수집 08/04 18:07 KST" 공통 배지 + "최대 20분...갱신 주기" 각주. SKHY DR: "07/31 종가" 확정 라벨. 메인에도 지연 표기. KRX 장중 시간대의 실수집 라벨은 실행 시각상 미실측 - 동일 코드 경로(SMSN 장중)로 실측했고 세션 판정은 #15로 검증 | PASS |
| 17 | 장중 실행 실패 처리 (완료 기준 5) | fetch_close_volume 몽키패치로 SKM만 실패 주입 -> run_intraday -> 정상 재실행 복구 | 실패 종목은 마지막 정상값 유지 + fetch_error, 나머지 정상, exit 2 | SKM fetch_error 기록 + 직전 스냅샷(-10.7694%) 유지, 타 10종 정상 갱신, exit 2. 복구 후 fetch_error 잔존 없음 | PASS |

### 3.4 F3: 일/주/월 단위

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 18 | 월 집계 - TSM MAX (완료 기준 1) | 기대값을 PRD 규칙(캘린더 월 마지막 거래일)으로 파이썬 독립 산출 -> 출하 app.js aggregate() 실행 결과와 대조 | 포인트 수 = 월 수, 표본 3개월 일치 | 270포인트 = 월 수 일치. 2026-05(05-29, +11.672)·2025-11(11-28, +26.7854)·2024-03(03-29, +12.2697) 전부 일치 | PASS |
| 19 | 주 집계 - KB 표본 3주 + 이월 승계 (완료 기준 2) | ISO 주(월~일) 마지막 거래일 기준 독립 산출 대조 | 값 일치 + 마지막 거래일이 이월가인 주는 플래그 승계 | 3/3 일치. 06-15 주(마지막 거래일 06-19 = 미 준틴스 ff_dr)에서 ff_dr=true 승계 확인. 툴팁 구간 라벨 "주: 07-20~07-24" 형식 확인 | PASS |
| 20 | 조합 규칙 (완료 기준 3) | UNIT_RULES 검사 | 1M/3M=일, 6M/1Y=일/주, MAX=일/주/월 | 완전 일치. 기간 변경 시 허용 외 단위는 D로 복귀하는 코드 확인 | PASS |
| 21 | 집계 안내 문구 (완료 기준 4) | 코드 확인 ("주/월 단위는 해당 기간 마지막 거래일 기준입니다" + 주/월 선택 시만 표시 로직) | 문구·조건 존재 | 코드 존재 확인. 화면 표시 실측은 BLOCKED #2 포함 | PASS |

### 3.5 F4: 전일 대비 (%p)

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 22 | 델타 수기 재계산 - WF/KT/LPL (개발팀 표본과 다른 3종목) | history 직전 확정 포인트로 수기 계산, meta와 대조 | +/-0.01%p, 기준일 = 직전 포인트 날짜 | 3/3 완전 일치 (차이 0.0000%p), prev_date 전부 08-03 일치 | PASS |
| 23 | 표시 형식·프리렌더 (완료 기준 2) | 원시 HTML 검사 | 부호·%p·색상, 상세 전체형 + 메인 축약형 + 기준일 | 상세 "(+0.19%p, 08/03 대비)" + pos/neg 클래스(본체 색상과 독립 적용 확인: KB 프리미엄 neg + 델타 pos), 메인 축약 "(+0.22%p)" + title="08/03 대비" 툴팁 | PASS |
| 24 | 직전 포인트 없음 케이스 (완료 기준 3) | 단위 테스트(F4 델타 3건, 81건에 포함) 재실행으로 판정 | 괄호 생략, 필드 null | 해당 단위 테스트 통과. 실데이터에는 해당 종목 없음(SKHY도 18행) | PASS |

### 3.6 F5: SMA·기간 평균

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 25 | SMA20/60 수기 대조 (완료 기준 1) | KB 20개/60개 평균 독립 계산 -> smaMap() 실행값 대조 (최신일 + 내부일 3표본) | +/-0.01%p | 3/3 일치 (SMA20@08-04 0.7421, SMA60@08-04 0.9818, SMA20@07-01 1.1357). n-1 이전 미정의 확인 | PASS |
| 26 | 기간 평균 - 1Y 수기 대조 + 패닝 재계산 (완료 기준 2) | 1Y 윈도우(258포인트) 평균 독립 계산 vs 윈도우 값 평균 | 일치, 윈도우 변경 시 재계산 | 0.4778 완전 일치. 평균은 표시 윈도우 values에서 매 렌더 계산되는 구조로 패닝 시 재계산됨(코드 + 윈도우 함수 검증) | PASS |
| 27 | 데이터 부족 (SKHY) | smaMap(18행, 20) | 빈 맵 + "데이터 부족" 비활성 | 빈 맵 확인. 토글 라벨 "SMA20 (데이터 부족)" 코드 확인 | PASS |
| 28 | 주/월 단위 SMA 비활성 (완료 기준 4) + 기본 꺼짐 (완료 기준 3) | 코드 검사 | unit != D 시 비활성, 기본 false | state 기본값 sma20/sma60/avg/vol 전부 false, updateToolbar의 unit!=="D" 비활성 분기 확인. UI 실측은 BLOCKED #2 | PASS |
| 29 | 수식 안내 정의 (완료 기준 5) | SKHY 페이지 수식 박스 검사 | SMA·기간 평균 정의 게재 | "SMA_n(d) = 직전 n개...단순 평균", "기간 평균 = 현재 화면에 표시 중인 구간...", 주/월 미표시 안내까지 게재 | PASS |

### 3.7 F6: 거래량

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 30 | 거래량 원천 대조 (완료 기준 1) | yfinance 독립 재조회 vs history JSON - SKHY DR 3일 + KB 원주 2일 (개발팀과 다른 조합 포함) | 일치 | 5/5 **완전 일치** (예: SKHY 07-28 51,088,300주) | PASS |
| 31 | 이월 포인트 거래량 null (완료 기준 3) | 실데이터 검사 (KB 06-19 ff_dr, SMSN 8/3·8/4 ff_dr) | 휴장 시장 거래량 null | KB 06-19 vol_dr=null·vol_local=2,181,977 / SMSN 이월일 vol_dr=null 확인. 바 미표시 코드(null skip) 확인 | PASS |
| 32 | 주 단위 합계 (완료 기준 3) | 주별 실거래일 합 독립 계산 vs aggregate() | 일치 + 툴팁 "(합계)" 표기 | 3주 표본 dr·원주 합계 전부 일치. 툴팁 "(합계)" 코드 확인 | PASS |
| 33 | 파일 크기·전 심볼 제공 | TSM 크기 + 22심볼 필드 (PRD 9절 기재) | 허용 범위 | TSM 1.07MB (설계 기록과 일치). 11종 전부 vol 필드 존재 | PASS |
| 34 | 토글·시리즈 전환·서브패널 겹침 **실조작** (완료 기준 1·2·4) | 브라우저 실측 필요 | - | BLOCKED #2 (단위 라벨·서브패널 분리 코드는 확인) | BLOCKED |

### 3.8 F7: 전환 현황 (최중요)

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 35 | 11개 DR 심볼 발행주식수 독립 재조회 (완료 기준 1) | yfinance info를 22심볼 직접 조회, 검증 A 판정 재계산 | PRD 9절 확인표대로 11종 전부 기각 대상 | **11/11 기각 확인** (원주환산/총주식 100.0~117.8% - 전부 전사 환산치. SMSN은 개발팀 기입 103.4%에서 117.8%로 변동했으나 기각 판정 동일) | PASS |
| 36 | 파이프라인 채택 경로·기각 기록 | meta.json conversion 검사 | SKHY만 manual, 10종 미확인, 기각 사유 기록 | SKHY n_dr_source=manual(1.779억 ADS, as_of 07-31) + 검증 A 기각 기록, 10종 전부 미채택+기각 기록(1~3건) | PASS |
| 37 | 검증 A/B 모의값 단위 테스트 (완료 기준 2) | 81건 재실행 (전사환산 기각/한도 초과 기각/정상 채택 3케이스 포함) | 통과 | 통과 | PASS |
| 38 | SKHY cv/lu/금액 수기 재계산 (완료 기준 4) | PRD 7.2 수식으로 독립 계산 vs meta | 비율 +/-0.1%p, 금액 유효숫자 3자리 | cv 2.5061%(= PRD 기재 약 2.51%), lu 100.0000%, V_dr $25,569,567,000, V_local 1,091,386,333,107,000원, limit_pct 2.5061% - 전부 완전 일치 | PASS |
| 39 | SKHY 화면 (완료 기준 3) | 원시 HTML 검사 | 한도 1,779만주 + 기준일 + 출처, 수동 입력 라벨, 게이지·소진율 | "전환한도: 원주 기준 17,790,000주 (기준일 2026-07-29, 출처: 회사 실적 컨퍼런스콜...)", "수동 입력 기준일 2026-07-31 (출처: ...)", 소진율 100% 게이지 | PASS |
| 40 | 통화 병기·축약 (완료 기준 5) | HTML 형식 검사 | 시장별 금액 상호 병기 | USD·KRW 병기, 조원/억 달러 축약 확인 | PASS |
| 41 | 정직성·비대칭 각주 (완료 기준 6) | HTML 검사 | 두 각주 노출 | "두 시장 금액의 합은 시가총액과 일치하지 않을 수..." + "ADR -> 원주 방향 전환은 자유롭지만, 원주 -> ADR 방향은 한도·신고 절차 제약(비대칭)" 둘 다 존재 | PASS |
| 42 | 미확인 폴백 (완료 기준 7) | SMSN·TSM·KB 페이지 검사 | "데이터 미확인" + 섹션 유지 + 한도 행 미표시 | 3/3 정상 (빈 값 노출 없음). 콘솔 에러 0건 확인은 BLOCKED #4 | PASS |
| 43 | 수식 안내 7.2 게재 (완료 기준 8) | 수식 박스 검사 | 정의 전부 | N_dr_local·전환율·소진율·DR/현지 시장 금액·기호 정의 전부 게재 | PASS |

### 3.9 F8: 시가총액

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 44 | 시총 수기 대조 (완료 기준 1) | SHG(KRX) + TSM(TWSE): MC = N_total * 종가 독립 계산 vs meta | 유효숫자 3자리 | 2/2 완전 일치 (SHG 48.17조원, TSM NT$60.16조) | PASS |
| 45 | 표시 형식·기준일 (완료 기준 1·2) | HTML 검사 | 현지통화 우선 + USD 병기 + "종가 기준" 라벨, F7과 동일 N_total 소스 | 시가총액 카드 + "종가 기준" 라벨 + 조원/NT$/달러 병기. N_total은 F7과 같은 shares 블록 사용(코드·데이터 확인). mcap 교차 검증 경고 로직 존재(이번 실행 경고 0건) | PASS |

### 3.10 브라우저 실조작 (BLOCKED 집계)

| # | 테스트 항목 | 사유 | 판정 |
|---|---|---|---|
| B1 | F1 드래그·터치 스와이프·키보드 실조작, "최신으로" 버튼 클릭, 패닝 중 콘솔 에러 | 브라우저 자동화 도구가 본 세션에서 미제공 (이전 회차에 사용한 도구 소실). 로직 계층은 #9~11로 검증 완료 | BLOCKED |
| B2 | F3/F5/F6 토글 UI 실조작 (비활성 상태 표시, 범례, 거래량 서브패널 렌더, 안내 문구 노출) | 동일. 상태·계산 로직은 #18~34로 검증 | BLOCKED |
| B3 | 375px 레이아웃 실측 (신규 툴바 포함 가로 오버플로) | 동일. CSS 브레이크포인트 유지는 정적 확인 | BLOCKED |
| B4 | 콘솔 에러 0건 (전 페이지·전 조작) | 동일 | BLOCKED |
| B5 | 기존 회귀 중 브라우저 의존 항목 재실측 (다크모드 4기준, 해시 리다이렉트 실행, 오류 주입 런타임 생존) | 동일. 관련 코드 경로 무변경 + 스크립트 존재 정적 확인 + 이전 회차 전체 PASS 이력 있음 | BLOCKED |

## 4. FAIL 항목 상세

없음. (1차 실행에서 나온 검사 실패 9건은 전부 QA 검사 스크립트 자체의 패턴 오류로 판명 - 비대칭 각주의 HTML 이스케이프 `-&gt;` 미고려 2건, 주차 매칭에 연도 미포함 7건. 수정 후 전 항목 통과. 제품 결함 아님)

## 5. 제약 확인·관찰 (FAIL 아님)

| # | 내용 |
|---|---|
| C-1 | (제약 13) 세션 표는 하계 고정 오프셋 - 모의 시각 15케이스로 현재(하계) 동작 정확성 확인. 동계 전환기 점검은 운영 체크리스트 과제로 유지 |
| C-2 | (제약 14) F7 자동 조회 전 종목 기각 - 독립 재조회로 재확인. SKHY 수동값은 "한도 전량 소진" 보도 기반 편집 판단이므로 분기 점검 필수. 참고: SMSN의 검증 A 판정값이 개발팀 기입(103.4%)과 재조회(117.8%) 간 변동 - Yahoo 필드 자체가 흔들린다는 방증으로, 자동 채택을 차단하는 현 설계가 타당함을 역설적으로 지지 |
| C-3 | (제약 6 관련 관찰) 검증 시점에 Yahoo가 미국 8/3 종가(SKHY·TSM 등)와 런던 8/3 종가(SMSN)를 NaN/미제공으로 반환 - 원천 데이터 지연. 파이프라인은 dropna + 이월(ff_dr)로 정확히 처리했고 라벨(07/31 종가)도 정직하게 표기됨. 결함 아님, 야후 단일 소스 제약의 실사례 |
| C-4 | (제약 15) 시총·전환 금액이 intraday 실행에서 갱신되지 않음 - process_ticker_intraday가 기존 엔트리를 유지하는 구조로 확인 (F8 방침 부합) |
| C-5 | (제약 16) F1 실조작 검증 한계 - 본 세션도 동일 환경 제약. BLOCKED 5건으로 명시 |
| C-6 | (관찰) 주 단위 툴팁 구간 라벨("주: 07-20~07-24")에 연도가 없어 MAX+주 뷰에서 다른 연도의 같은 주차와 라벨 문자열이 동일 - 단 날짜 본문(YYYY-MM-DD)이 연도를 제공하므로 PRD 형식과는 일치. 개선 여지 수준 |

## 6. 종합 판정

- **PASS 43 / FAIL 0 / BLOCKED 5** (BLOCKED은 고유 항목 기준 - 3.2절 #12와 3.7절 #34는 3.10절 B1·B2와 동일 건의 상호참조)
- **실행 가능한 전 항목 PASS.** F7 최중요 항목은 11심볼 독립 재조회로 검증 A 전량 기각을 재확인했고, SKHY 전환율 2.5061%(약 2.51%)·소진율 100%·시장별 금액이 PRD 7.2 수식 수기 계산과 완전 일치, 폴백·각주·출처 라벨 전부 확인. F2는 history 해시 불변·차트 분리(장중가 스냅샷 격리)·세션 판정 15케이스·실패 주입까지 실행 검증. F3/F5/F6 계산은 출하된 app.js를 Node에서 실데이터로 실행해 PRD 규칙 독립 산출값과 34항목 전부 일치. 거래량은 야후 독립 재조회와 완전 일치. 기존 회귀(단위 81, 실데이터 검증, SEO 92항목) 전체 유지.
- **BLOCKED 5건은 전부 브라우저 실조작 항목** - 개발팀 1차 테스트가 미확인으로 남긴 바로 그 영역이며, 본 세션에서도 브라우저 자동화 도구 부재로 실측 불가. 로직 계층 검증으로 실조작 실패 위험은 낮다고 평가하나, **F1 드래그·터치의 실기기 체감(임계값 10px의 적절성 포함)과 375px 툴바 레이아웃은 코드 검증으로 대체 불가능**하므로, 브라우저 사용 가능 환경(또는 사용자 수동 확인)에서의 후속 실측을 권고한다.
- 잔존 과제 (결함 아님): C-1 동계 세션 표 점검, C-2 SKHY 수동값 분기 점검, 기획팀의 타 종목 전환한도 조사(PRD 11절-5).

---
*작성: 테스트팀, 2026-08-04. 검증 스크립트: `docs/testing/scripts/2026-08-04-qa-v2-verify.py`(F2/F4/F7/F8), `2026-08-04-qa-v2-calc.py`(F3/F5/F6 기대값 독립 산출), `2026-08-04-qa-appjs-harness.js`(출하 app.js Node 실행 대조), `2026-08-04-qa-v2-misc.py`(세션 시뮬레이션·거래량 원천 대조·WCAG·워크플로). 기존 스크립트의 프로젝트 경로를 폴더명 변경("금융도구 웹사이트" -> "Stock tools")에 맞춰 갱신함. 본 리포트의 수치는 검증 시점 조회 기준이며 투자 조언이 아님.*
