# SEO 필수 구현(D1~D5) 테스트 리포트

## 이력

| 날짜 | 회차 | 결과 요약 |
|---|---|---|
| 2026-08-04 | 1차 | PASS 30 / FAIL 4 / BLOCKED 1. 프리렌더·메타태그·sitemap·JSON-LD·리다이렉트·기능 회귀는 견고함. FAIL은 (1) 가이드 마크다운 렌더 변형 (2) FAQ JSON-LD 답변 오염 (3) 메인 title 템플릿 불일치 (4) 로컬 갱신 배치 실행 불가 |
| 2026-08-04 | 2차 | PASS 11 / FAIL 0 / BLOCKED 0. FAIL #1·#2·#4 수정 확인, FAIL #3은 관리자 판정(마케팅 문서 정정)으로 종결 - 정합 확인. **범위 내 전체 PASS 확정** (GitHub Actions 실행만 저장소 생성 후 과제로 잔존, 7절 참조) |

## 1. 테스트 대상

- 코드 (신규): `src/build_pages.py`, `src/config/site.json`, `src/tests/test_build_pages.py`, `.github/workflows/update-data.yml`, `src/scripts/update_data.bat`, `src/scripts/register_task.ps1`
- 코드 (수정): `src/web/app.js` (하이드레이션 재작성), `src/web/styles.css` (가이드·표 스타일)
- 코드 (무변경 주장 검증): `src/fetch_data.py`, `src/config/tickers.json`, `src/tests/test_premium.py`, `src/tests/verify_samples.py` - 1차 리포트 시점 SHA-256과 대조해 **무변경 확인** (계산 로직 재검증 불필요 판단 유지)
- 생성물: `src/web/` 14페이지(메인 1 + 종목 11 + 가이드 2) + sitemap.xml + robots.txt
- 요구사항 스펙: `docs/marketing/2026-08-03-adr-premium-marketing.md` 2.1~2.4절, 2.6절 D1~D5
- 가이드 원고 원본: `docs/marketing/guides/adr-premium.md`, `adr-conversion.md`
- 설계 문서: `docs/development/2026-08-03-adr-premium-design.md` (2026-08-04 갱신본. 7절 제약 명시 항목은 제약 확인으로 분류)

## 2. 테스트 환경과 실행 방법

- 환경: Windows 11 Home, Python 3.11.9, yfinance 1.5.2. 브라우저는 뷰포트 강제(1280x800/768x1024/375x812) + colorScheme 에뮬레이션. YAML 검증용 PyYAML 신규 설치(테스트 도구, 소스 무관)
- 실행 절차:
  1. `python -m unittest discover -s src/tests -p "test_*.py"` (46건)
  2. D5 로컬 배치 `src\scripts\update_data.bat` 실제 실행 (결과: FAIL #4)
  3. 진단용 CRLF 사본(스크래치패드)으로 파이프라인 재실행 - fetch 11/11 + build 14페이지 성공. 이후 모든 콘텐츠 검증은 이 시점(2026-08-04 08:41 KST) 데이터·페이지 기준
  4. `python docs/testing/scripts/2026-08-04-qa-seo-verify.py` - D1~D4 파일 레벨 전수 검증 (원시 HTML을 파일로 직접 읽음 - JS 미실행 크롤러 뷰. 종목 11페이지 x 15항목, 메타태그 14페이지, sitemap, JSON-LD 파싱)
  5. 브라우저 실측: 해시 리다이렉트 4종, 하이드레이션 회귀(차트·탭·툴팁), 다크모드 4기준, 반응형 3구간, 오류 주입(KB.json 임시 제거 - 원상복구 완료), 콘솔
  6. WCAG 대비: 신규 가이드 스타일 8조합 계산
- 오류 주입·로그 파일 등은 전부 원상복구했다 (history/KB.json 복원 검증, update_data.log 삭제). 소스 코드는 수정하지 않았다. register_task.ps1은 관리자 지시대로 **실행하지 않고** 구문 파싱 검사만 수행했다.

## 3. 항목별 결과

### 3.1 단위 테스트·파이프라인

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 1 | 단위 테스트 46건 재실행 | unittest discover | 46/46 통과 | 46/46 OK (기존 16 + 프리렌더 30) | PASS |
| 2 | fetch + build 파이프라인 전체 실행 | 진단 배치 경유 실행 (2절-3) | fetch 11/11, build 14페이지+sitemap+robots | 전부 성공. build 1초 미만. 프리렌더 수치가 새 meta.json과 일치함을 #5~#6에서 전수 확인 | PASS |
| 3 | 계산 로직 무변경 검증 | fetch_data.py 등 4개 파일 SHA-256을 1차 리포트 시점과 대조 | 무변경 | 4/4 UNCHANGED - 이전 회차 계산 검증 유효 | PASS |

### 3.2 D1: 프리렌더 (JS 미실행 원시 HTML)

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 4 | 메인 원시 HTML 핵심 콘텐츠 | index.html 파일 직접 읽기, meta.json과 수치 대조 | 11카드 전부: 종목명·프리미엄 %·DR/원주 가격·기준시점·카드 순서·스파크라인 SVG(0%선)·가이드 링크·고지·생성 시각 | 11/11 카드 수치 = meta.json 일치 (예: SKHY +18.73%), 순서 일치, 스파크 path 11 + 0% 점선 11, 전부 텍스트로 존재 | PASS |
| 5 | 종목 11페이지 원시 HTML 15항목 전수 | 각 페이지 파일 읽기 x 15항목(종목명·프리미엄·원천값 4종과 각 기준일·전환비율 표기·수식·고지·원주 환산가·noscript·기간 탭·하이드레이션 훅) | 전 항목 존재, 수치 = meta.json | 11페이지 x 15항목 = 165개 전부 통과 | PASS |
| 6 | SMSN GDR 표기 | smsn 페이지 + 타 10종 페이지 검사 | SMSN에만 GDR(런던) 배지·안내 | SMSN 존재, 타 10종 부재 - 정확 | PASS |
| 7 | 갱신 실패 프리렌더 | meta 사본에 KB fetch_error 주입 후 build_all을 임시 폴더로 실행 | 카드 "갱신 실패, OO 기준" + 상세 "마지막 정상값" + 마지막 정상 수치 유지 | 3/3 프리렌더 확인 (+1.87% 유지) | PASS |

### 3.3 가이드 페이지 (원고 대조)

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 8 | 가이드 2편 생성·구조 | 생성 HTML과 원고 md 대조 | H1=원고 title, 섹션 전부, 전환비율 표 12행(헤더+11종목), 링크 a 태그, 말미 고지 | 생성·표(12행, th 5)·링크·고지·FAQ 질문 원고 일치. 단 본문 렌더 변형은 #9 | PASS |
| 9 | 마크다운 변환 정확성 (내용 변형 없음) | adr-premium 생성 HTML 정밀 검사 | 원고가 변형 없이 렌더링 | **3종 변형 발견**: (1) ``` 코드 펜스 미지원 - 백틱 잔재 "``" 4개 노출, 수식 두 줄이 한 줄로 병합, 수식 내 `*`가 `<em>`으로 소비되어 곱셈 기호 소실 (2) 여러 줄 목록 항목이 문장 중간에서 잘려 `<p>`로 분리, `<ul>/<ol>` 내부에 `<p>` 중첩(비유효 HTML) - 양 가이드 합계 10개 조각 (3) FAQ 가시 답변("계산하나요")에도 `*` -> `<em>` 변형 | FAIL |

### 3.4 D2: 메타태그

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 10 | canonical·OG 일관성 (14페이지 전수) | 각 페이지 head 파싱 | canonical = base_url+경로(자기 참조), og:url=canonical, og:title=title, og:description=description, og:site_name | 14/14 통과. og:type: 종목·메인 website / 가이드 article | PASS |
| 11 | title 중복 없음 | 14페이지 title 집합 비교 | 전부 고유 | 14개 전부 고유, 전부 사이트명으로 끝남 | PASS |
| 12 | 종목·가이드 title/description 템플릿 | 마케팅 2.3절 표와 문자열 완전 일치 비교 (SKHY·SMSN·TSM 오버라이드, KB 기본 템플릿, 가이드는 원고 meta_description) | 완전 일치 | SKHY·SMSN·TSM title/desc 완전 일치, KB "KB금융 ADR 프리미엄(KB 괴리율) 차트 - ..." + desc에 원주 코드·전환비율 포함, 가이드 desc = 원고 머리말 | PASS |
| 13 | 메인 title 템플릿 | 마케팅 2.3절 "한국·대만 ADR 프리미엄 **실시간** 추적 - [사이트명]"과 비교 | 완전 일치 | 생성본 "한국·대만 ADR 프리미엄 추적 - ..." - **"실시간" 누락 (불일치)**. 4절 FAIL #3 (참고: MVP는 일별 종가라 "실시간"이 오히려 부정확 - 문구 확정 주체 합의 필요) | FAIL |

### 3.5 D3: sitemap / robots

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 14 | sitemap.xml 완전성 | URL 목록 vs 실존 페이지 집합 비교 + XML 파싱 | 14 URL = 실존 14페이지, lastmod 형식 | 14/14 정확히 일치(집합 비교), lastmod=2026-08-03(meta 날짜, W3C 형식), ElementTree 파싱 성공 | PASS |
| 15 | robots.txt | 내용 검사 | User-agent/Allow/Sitemap 절대경로 | 3항목 전부 유효 | PASS |

### 3.6 D4: JSON-LD

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 16 | 메인 WebSite+Organization | ld+json 파싱 | 두 타입, name·url 정확 | @graph에 둘 다 존재, name=사이트명, url=base_url | PASS |
| 17 | 종목 11페이지 BreadcrumbList | 파싱 + 필수 필드 | 홈>종목명 2단계, position·item URL 정확 | 11/11 유효 | PASS |
| 18 | 가이드 FAQPage 구조·질문 | 파싱, 원고 Q&A와 대조 | 원고에 FAQ 있을 때만 생성, 질문 일치, Question/Answer 필수 필드 | adr-premium 5문항·adr-conversion 3문항 질문 전부 원고 일치, 필수 필드 유효, "계산하나요" 답변 원문 일치 | PASS |
| 19 | FAQ 답변 순수성 | 답변 텍스트에 원고 외 내용 유입 검사 | 답변 = 원고 A. 텍스트만 | **양 가이드의 마지막 FAQ 답변에 원고 말미의 기울임 면책 문구("*본 글은 정보 제공 목적이며...*")가 리터럴 별표까지 포함된 채 붙음**. 4절 FAIL #2 | FAIL |

### 3.7 해시 리다이렉트

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 20 | #/SKHY (대문자 구 URL) | localhost:8765/#/SKHY 문서 로드 | /stocks/skhy/ 로 이동 | location.replace로 이동, SEO title 적용 확인 | PASS |
| 21 | #/skhy (소문자) | 동일 | /stocks/skhy/ | 정상 이동 | PASS |
| 22 | #/XXXX (미지 티커) | 동일 | 리다이렉트 없음, 페이지 정상 | 메인에 잔류, 카드 11개 정상 렌더 (설계 의도 명시와 일치) | PASS |
| 23 | 해시 없음 + 종목 페이지 위 구 해시 | / 일반 로드, /stocks/skhy/#/TSM 로드 | 각각 정상 / /stocks/tsm/ 이동 | 메인 정상. 교차 페이지 해시도 ../../ 상대 경로로 정확히 /stocks/tsm/ 이동 | PASS |

### 3.8 기능 회귀 (하이드레이션 재작성 후)

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 24 | 차트 하이드레이션 + 기간 탭 | /stocks/kb/ 에서 탭 5종 클릭, X축 범위 판독 | 1M/3M/6M/1Y/MAX 범위 정확 | 1M 07-03~, 3M 05-04~, 6M 02-02~, 1Y 2025-08-04~, MAX 2003-12-01~ (전부 정확) | PASS |
| 25 | 툴팁 일반·이월 | KB 3M에서 2026-06-19(미 준틴스)·06-18 mousemove | 이월일 "(이월)+휴장 이월: DR 이월가", 일반일 표기 없음, 원천값 표시 | 두 케이스 모두 정확, 이월값 = 직전 거래일 DR $106.77, 이탈 시 숨김 | PASS |
| 26 | 다크모드 (a)(b)(c) | localStorage 초기화 + OS 다크 에뮬레이션 + 리로드, 토글, 페이지 이동 | (a) 첫 진입 다크 (b) 토글 즉시 (c) 저장값이 시스템 설정보다 우선 - 페이지 간 유지 | (a) dark 렌더 (b) 즉시 light + 저장 (c) 시스템 다크 + 저장 light 상태로 가이드 페이지 이동 시 light 유지. 토글은 전 페이지 유형에 존재 | PASS |
| 27 | 다크모드 (d) + 신규 스타일 WCAG | 가이드 링크(accent 텍스트)·코드/표 헤더·인용구 등 신규 8조합 대비 계산 | 텍스트 4.5:1 | 8/8 통과 (최저: 라이트 guide-list 링크 4.56:1). 기존 조합은 이전 회차 26/26 통과 유지 (색상 토큰 무변경 확인) | PASS |
| 28 | 반응형 375/768/1280 | 메인·종목·가이드에서 열 수·가로 오버플로 판독 | 3/2/1열, 오버플로 없음, 가이드 표는 내부 스크롤 | 1280 3열 / 768 2열 / 375 1열, 오버플로 0건. 가이드 전환비율 표(12행)는 375px에서 table-wrap 내부 가로 스크롤로 처리 | PASS |
| 29 | 오류 주입: 히스토리 부재 | history/KB.json 임시 제거 -> /stocks/kb/ 로드 -> 복원 | 프리렌더 콘텐츠(프리미엄·원천값 4종) 생존 + 차트 영역만 오류 안내 | 프리미엄 +1.87%·소스 4카드·고지 정상 표시, 차트 영역 "차트 데이터를 불러오지 못했습니다 (HTTP 404...)" - 페이지 생존. 복원 검증 완료 | PASS |
| 30 | 콘솔 에러 | 정상 플로우 후 오류 조회 | 0건 | 0건 | PASS |

### 3.9 D5: 자동 갱신

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| 31 | workflow YAML 유효성·명령 일치 | PyYAML 파싱 + 스텝 검토 | 문법 유효, 실행 명령 = 실제 파이프라인 | 파싱 성공. cron "30 21 * * *"(KST 06:30), 스텝 checkout -> setup-python 3.11 -> pip install yfinance -> fetch_data.py -> build_pages.py -> unittest -> 조건부 커밋(git add src/web), permissions contents:write - 실제 실행 절차와 일치 | PASS |
| 32 | update_data.bat 로컬 실행 | 배치 직접 실행 | fetch + build 수행, 로그 누적 | **실행 실패** - cmd가 명령을 오파싱해 파이프라인이 전혀 실행되지 않음 (meta.json 무변경으로 확인). 4절 FAIL #4 | FAIL |
| 33 | register_task.ps1 | 구문 파싱 검사만 (실행은 관리자 지시로 금지) | 구문 유효 | PSParser 구문 오류 0건. schtasks 명령 구성 타당. 단 등록 대상인 bat가 FAIL #4 상태라 수정 전 등록은 무의미 | PASS |
| 34 | GitHub Actions 실제 실행 | - | 스케줄 실행 검증 | git 저장소가 아니어서 실행 불가 (설계 문서에 "저장소 생성 후 활성화 준비물"로 명시) | BLOCKED |

## 4. FAIL 항목 상세

### FAIL #1: 가이드 마크다운 변환기가 실원고를 변형 렌더링 (심각도: 중간)

- 재현 절차: `python src/build_pages.py` 실행 후 `src/web/guide/adr-premium/index.html`을 열어 "계산법 (수식)" 섹션과 "해석 시 주의점" 섹션을 확인. 또는 브라우저에서 /guide/adr-premium/ 접속.
- 기대 결과: 원고(`docs/marketing/guides/adr-premium.md`)의 코드 블록·목록이 원문대로 렌더링.
- 실제 결과 (3종 변형):
  1. **``` 코드 펜스 미지원**: md_to_html()이 펜스 라인을 일반 문단으로 취급. 결과적으로 (a) 백틱 잔재 "``"가 화면에 4곳 노출 (b) 수식 두 줄이 한 문단으로 병합 (c) 수식의 `*`(곱셈)가 기울임 마크다운으로 소비되어 `<em>` 태그가 되고 곱셈 기호 자체가 사라짐. 렌더 예: `ADR 이론가(달러) = (P_local <em> r) / FX ... </em> 100` - **계산법을 설명하는 SEO 랜딩의 핵심 수식이 깨진 채 노출**된다.
  2. **여러 줄 목록 항목 분리**: 원고의 목록 항목이 여러 줄로 개행되어 있으면(관례적 마크다운) 첫 줄만 `<li>`에 남고 이어지는 줄이 문장 중간에서 잘려 `<p>`로 분리된다. `<ul>/<ol>` 내부에 `<p>`가 중첩되는 비유효 HTML도 생성. 양 가이드 합계 10개 조각 (adr-premium의 "왜 생기나" ul 3개 + "주의점" ol 4개, adr-conversion의 "바뀔 수 있다" ul 3개).
  3. FAQ 가시 답변에도 동일한 `*` -> `<em>` 변형 ("어떻게 계산하나요" 답변의 수식).
- 관련 파일: `src/build_pages.py`의 `md_to_html()`, `_INLINE_PATTERNS`. 원고: `docs/marketing/guides/*.md`
- 수정 제안: (1) ``` 펜스 블록을 `<pre><code>`로 변환(펜스 내부는 인라인 패턴 미적용) (2) 목록 항목의 연속 줄(들여쓰기 또는 비마커 줄)을 직전 `<li>`에 이어붙이기 (3) 단위 테스트에 "여러 줄 목록·코드 펜스" 케이스 추가. 참고: 설계 제약 11이 "새 형식 변형이 오면 파서 보강 필요"라 했으나, 코드 펜스와 여러 줄 목록은 **이미 수신된 실원고에 존재**하므로 제약이 아니라 결함으로 판정.

### FAIL #2: FAQ JSON-LD 마지막 답변에 원고 말미 면책 문구 유입 (심각도: 낮음~중간)

- 재현 절차: `src/web/guide/adr-premium/index.html`의 `<script type="application/ld+json">`을 파싱해 mainEntity 마지막 Question의 acceptedAnswer.text를 확인 (adr-conversion도 동일).
- 기대 결과: 답변 = 원고의 해당 A. 텍스트만.
- 실제 결과: 두 가이드 모두 마지막 답변 끝에 원고 말미의 기울임 면책 문구가 리터럴 별표 포함으로 붙음. 예: "...본 사이트는 투자 판단을 제공하지 않습니다. *본 글은 정보 제공 목적이며 투자 조언이 아닙니다. ...*"
- 원인: md_to_html()의 FAQ 수집(cur_a)이 `---` 가로줄에서 종료되지 않아, FAQ 섹션 이후의 말미 문단까지 답변으로 흡수.
- 관련 파일: `src/build_pages.py` `md_to_html()` (flush_faq 호출 지점 - hr 처리 분기에 없음)
- 수정 제안: `<hr>` 처리 분기에 flush_faq() 추가 (또는 H2 외 구조 요소에서 FAQ 컨텍스트 종료). 구조화 데이터 리치결과 심사에서 불리할 수 있어 공개 전 수정 권장.

### FAIL #3: 메인 title이 마케팅 2.3절 템플릿과 불일치 (심각도: 낮음 - 합의 필요)

- 재현 절차: `src/web/index.html`의 `<title>` 확인.
- 기대 결과 (마케팅 2.3절): "한국·대만 ADR 프리미엄 **실시간** 추적 - [사이트명]"
- 실제 결과: "한국·대만 ADR 프리미엄 추적 - ADR 프리미엄 트래커" - "실시간" 누락. 나머지 13페이지의 title·description은 템플릿과 완전 일치.
- 관련 파일: `src/build_pages.py`의 `MAIN_TITLE`
- 비고: MVP는 일별 종가 기반이라 "실시간" 표기는 오히려 부정확·과장 소지가 있다(메인 화면 자체도 "지연 데이터" 표기). 개발팀이 의도적으로 뺐을 가능성이 높으나 **어느 문서에도 결정 기록이 없다**. 코드를 고칠 게 아니라 마케팅 문서 2.3절을 개정하는 방향이 타당해 보이며, 확정 주체(마케팅팀-관리자) 합의 후 어느 한쪽을 갱신해야 스펙-구현 불일치가 해소된다.

### FAIL #4: update_data.bat 실행 불가 - LF 전용 줄바꿈 (심각도: 중간, D5 로컬 대안 무력화)

- 재현 절차: `src\scripts\update_data.bat` 실행 (PowerShell `& ...bat` 또는 더블클릭).
- 기대 결과: fetch_data.py + build_pages.py 실행, 로그 누적.
- 실제 결과: cmd가 "'Windows'은(는) ... 명령이 아닙니다", "'h_data'...", "The system cannot find the path specified" 등을 뿜으며 **파이프라인이 전혀 실행되지 않음** (실행 전후 meta.json generated_at 무변경으로 확인).
- 원인: 파일이 **LF 전용 줄바꿈**(CRLF 0개)으로 저장됨 - cmd.exe 배치 파서는 CRLF를 전제하므로 라인을 오분할한다. (UTF-8 한글 주석도 cp949 콘솔에서 모지바케를 유발하나, 실행 실패의 직접 원인은 줄바꿈이다.)
- 근본 원인 검증: 동일 로직을 CRLF + ASCII 주석으로 저장한 진단 사본(스크래치패드)은 정상 실행되어 fetch 11/11 + build 14페이지 성공 (재현 스크립트: `docs/testing/scripts/2026-08-04-qa-make-diag-bat.py`).
- 관련 파일: `src/scripts/update_data.bat`. 연쇄 영향: `register_task.ps1`이 이 bat를 스케줄러에 등록하므로, bat 수정 전에는 로컬 자동 갱신 전체가 동작하지 않는다.
- 수정 제안: CRLF로 재저장(필수). 한글 주석·echo는 ASCII로 바꾸거나 파일을 cp949로 저장(권장 - 콘솔 모지바케 방지). 수정 후 실제 실행으로 로그 생성까지 확인할 것.

## 5. 제약 확인 (설계 문서 7절 - FAIL 아님)

| # | 제약 | 확인 내용 |
|---|---|---|
| C-1 | base_url 플레이스홀더 (제약 9) | canonical·OG·sitemap·robots 전부 `https://adr-premium.example.com` 기준으로 생성됨을 확인. 도메인 확정 후 site.json 교체 + build 재실행이 배포 전 필수 |
| C-2 | 프리렌더 수치는 빌드 시점 스냅샷 (제약 10) | fetch만 실행하고 build를 생략하면 페이지 수치와 데이터 JSON이 어긋남 - 갱신 도구(워크플로·bat)가 두 단계를 함께 실행하도록 구성돼 있음을 확인 (단 bat 자체는 FAIL #4) |
| C-3 | Actions는 저장소 생성 후 활성화 (제약 2) | #34 BLOCKED와 동일. 60일 무활동 비활성화 유의사항이 워크플로 주석에 기재됨 |
| C-4 | 가이드 파서는 최소 부분집합 (제약 11) | 제약으로 인정되는 범위는 "미지원 문법(이미지·각주 등)"까지이며, 이미 수신된 원고가 쓰는 문법(코드 펜스·여러 줄 목록)의 오렌더는 FAIL #1로 판정 |
| C-5 | 개장 중 시장 당일 값 (제약 3) | 8/4 08:41 수집분에서 SMSN last_date가 2026-07-31로 후퇴(런던 8/3 종가가 야후에 아직 미반영) - 제약 범위 내 동작으로 확인 |

## 6. 종합 판정

- **전체 PASS 여부: 아니오 (FAIL 4건)**
- 견고한 부분: 프리렌더 코어(D1 - 종목 11페이지 165항목 + 메인 전수 일치), 메타태그(D2 - 메인 title 1건 외 완전 일치), sitemap/robots(D3 완전 일치), JSON-LD 구조(D4 - 답변 오염 1건 외 유효), 해시 리다이렉트 4종, 기능 회귀 0건(차트·탭·툴팁·다크모드·반응형·오류 생존·콘솔), 단위 테스트 46/46.
- 심각도 순 결함:
  1. **FAIL #4** update_data.bat 실행 불가 (LF 줄바꿈) - D5 로컬 자동 갱신이 통째로 무력. CRLF 재저장으로 즉시 수정 가능.
  2. **FAIL #1** 가이드 마크다운 렌더 변형 - SEO 랜딩(가이드)의 핵심 수식이 깨진 채 노출 + 비유효 HTML. 파서 보강 필요.
  3. **FAIL #2** FAQ JSON-LD 마지막 답변 오염 - 리치결과 심사 리스크. 한 줄 수정(hr에서 flush_faq).
  4. **FAIL #3** 메인 title "실시간" 누락 - 코드 수정이 아니라 마케팅 문서 개정이 타당할 수 있음. 합의 후 문서 또는 코드 한쪽 갱신.
- 재검증 범위 제안: FAIL #1·#2 수정 시 가이드 2편 재생성 후 본 리포트 3.3절·3.6절 항목 재실행(`2026-08-04-qa-seo-verify.py` 포함). FAIL #4 수정 시 bat 실제 실행 + 로그 확인. FAIL #3은 문서 합의 결과 확인. 계산·데이터·나머지 UI는 재검증 불필요.

## 7. 2차 회차 재검증 (FAIL #1·#2·#4 수정 확인, 2026-08-04)

개발팀 수정: (1) build_pages.py 파서 보강 - ``` 펜스를 pre/code로 변환(내부 마크다운 변환 배제), 여러 줄 목록 항목 버퍼링 병합, 기울임 정규식을 비공백 경계 규칙으로 조정, styles.css에 pre 스타일 추가 (2) 가로줄 분기에 flush_faq() 추가 (3) update_data.bat를 CRLF + ASCII 전용으로 재작성. 단위 테스트 54건(파서 회귀 8건 추가). FAIL #3은 관리자 판정으로 종결 - 마케팅 문서 2.3절에서 "실시간"을 삭제(이력 기록 확인)했으므로 코드-문서 정합만 확인.

재검증 범위는 1차 리포트 6절 제안 범위를 따랐고, 계산·데이터 코드 무변경(해시 대조)을 확인해 계산 재검증은 생략을 유지한다.

| # | 테스트 항목 | 절차 | 기대 결과 | 실제 결과 | 판정 |
|---|---|---|---|---|---|
| R2-1 | 단위 테스트 54건 | unittest discover | 54/54 | 54/54 OK (파서 회귀 8건 포함) | PASS |
| R2-2 | update_data.bat 독립 실행 (FAIL #4) | 배치 직접 실행, 전후 meta.json generated_at 대조 | 파이프라인 실행, 타임스탬프 갱신, exit 0 | CRLF 16줄·ASCII 전용 확인. 실행 결과 exit 0, fetch 11/11 + build 14페이지, generated_at 2026-08-04T00:41:54Z -> T00:49:13Z (실제 갱신 입증). 로그 파일 정상 누적 | PASS |
| R2-3 | 코드 펜스 렌더 (FAIL #1-1) | adr-premium 생성 HTML의 pre/code 블록을 원고 수식과 문자 단위 대조 | 백틱 잔재 0, 수식 원문 보존(곱셈 * 유지, 줄바꿈 유지), 펜스 내부 마크다운 변환 없음 | pre/code 2블록, "프리미엄(%) = (P_dr * FX / (P_local * r) - 1) * 100" 등 원문 그대로(개행 포함), 백틱 잔재 0, 내부 em/strong 없음. 브라우저 DOM에서도 동일 확인 | PASS |
| R2-4 | 목록 병합 (FAIL #1-2) | 양 가이드의 ul/ol 내부 p 중첩 검사 + 여러 줄 항목 병합 확인 | p 중첩 0, 항목이 한 li로 완결 | 양 가이드 p-in-list 0개 (1차 10개). "거래시간이 겹치지 않는다" ol 항목과 "전환 제약" ul 항목이 문장 끝까지 한 li로 병합됨 | PASS |
| R2-5 | FAQ 가시 답변 수식 보존 (FAIL #1-3) | "어떻게 계산하나요" 가시 답변 문단 검사 | `*` 보존, em 변환 없음 | "프리미엄(%) = (ADR 가격 * 환율 / (원주 가격 * 전환비율) - 1) * 100 입니다" 원문 그대로 | PASS |
| R2-6 | FAQ JSON-LD 오염 제거 (FAIL #2) | 양 가이드 마지막 답변 텍스트 검사 | 말미 면책 문구 미유입 | 양 가이드 모두 원고 A. 텍스트로 끝남, "본 글은 정보 제공" 미포함. 말미 기울임 면책 문구는 본문에는 em으로 정상 렌더(유실 없음) | PASS |
| R2-7 | FAQ 구조 재확인 | FAQPage 파싱, 문항·수식 답변 원문 대조 | 5+3문항, 필수 필드, 수식 답변 = 원고 완전 일치 | 전부 일치 | PASS |
| R2-8 | 메인 title 정합 (FAIL #3 종결 확인) | 정정된 마케팅 2.3절과 생성 title 비교 | "한국·대만 ADR 프리미엄 추적 - [사이트명]" 일치 | 마케팅 문서 이력에 관리자 정정 기록 확인, 생성 title과 완전 일치 | PASS |
| R2-9 | D1~D4 전수 재실행 | `2026-08-04-qa-seo-verify.py` (정정 템플릿 반영판) | 전 항목 통과 | **92/92 PASS** (종목 11p x 15항목, 메타태그 14p, sitemap 14 URL, JSON-LD 전수, FAQ 순수성·수식 답변 원문 일치 포함) | PASS |
| R2-10 | 재생성 부수 회귀 스팟 | /stocks/skhy/ 프리렌더 수치 vs meta.json, 차트 하이드레이션, 가이드 브라우저 렌더, 콘솔 | 수치 일치, 회귀 없음, 콘솔 0 | SKHY +30.57% = meta.json 일치, 차트·탭 5·소스 4카드 정상, 가이드 pre 2블록 DOM 확인, 콘솔 에러 0건 | PASS |
| R2-11 | 계산 코드 무변경 | fetch_data.py SHA-256 대조 | 무변경 | 99971c2901ab (1차와 동일) - 계산 재검증 생략 유지 | PASS |

**2차 회차 결과: PASS 11 / FAIL 0 / BLOCKED 0.**

관찰 (결함 아님):
- update_data.log에 파이프라인의 한글 출력이 모지바케로 기록된다 (Python UTF-8 출력 vs 콘솔 코드페이지 차이). 명령 실행·[OK] 판독에는 지장 없음. 개선 원하면 배치에 `chcp 65001` 추가 또는 `PYTHONIOENCODING=utf-8` 설정.
- 2차 검증 중 bat 실행이 KRX 개장 시간(09:49 KST)과 겹쳐 당일 값이 진행 중 가격으로 수집됨 - 설계 제약 3 범위 내.

### 최종 종합 판정 (2차 회차 기준)

- **범위 내 전체 PASS.** 1차 FAIL 4건 전부 종결 (#1·#2·#4 수정 확인, #3 관리자 판정·문서 정정으로 정합 확인). D1~D5 요구사항 중 실행 가능한 전 항목이 검증됐다.
- 잔존 BLOCKED 1건(1차 #34): GitHub Actions 실제 실행은 git 저장소 생성 후에만 가능 - 저장소 생성 시점에 워크플로 1회 수동 실행(workflow_dispatch)으로 확인할 것을 권고.
- 배포 전 필수 잔여 과제 (기능 결함 아님, 1차 5절 제약 확인): site.json base_url 플레이스홀더 교체 + build 재실행, 상업 데이터 API 전환 (PRD 6절).

---
*작성: 테스트팀, 2026-08-04 (2차 회차 동일). 검증 스크립트: `docs/testing/scripts/2026-08-04-qa-seo-verify.py` (D1~D4 파일 레벨 전수 - 2차에서 정정 템플릿·FAQ 검사 정밀화 반영), `docs/testing/scripts/2026-08-04-qa-make-diag-bat.py` (1차 FAIL #4 근본 원인 진단). 본 리포트의 수치는 검증 시점 조회 기준이며 투자 조언이 아님.*
